# 一键部署验证指南（步骤 2 真机对话验收用）

> 目标：把本仓库部署到有公网的免费环境，亲手验证 DeepSeek 真实对话。
> 全程点鼠标即可，不需要终端。

## 0. 先换 Key（重要）

之前贴在聊天里的 Key 已暴露，请先去 https://platform.deepseek.com/api_keys **删除旧 Key 并新建一个**。
下面部署时填写的是**新 Key**，不要再贴到聊天里。

## 1. 部署（Render 免费版，约 5 分钟）

1. 打开 https://render.com ，用 GitHub 账号登录。
2. 点 **New → Blueprint**，选择仓库 `bing876/work`，分支选 **`arena/01a08a13-work`**，点 Apply。
   （仓库根目录的 `render.yaml` 会被自动识别，只有一个服务 `dimspace`。）
3. 在 Environment 页填写 **`MODEL_API_KEY`**（粘贴新 Key），再加一个 **`AUTH_SECRET`**（随便填一长串乱码，当登录钥匙，改了会导致所有人掉线重登），其余保持默认，点 **Deploy**。
4. 等待构建完成（约 3–5 分钟，日志最后出现 `listening` 即成功）。
5. 打开分配的地址（形如 `https://dimspace-xxxx.onrender.com`）。

说明：免费版 15 分钟无人访问会休眠，首次打开需等约 1 分钟唤醒；未挂硬盘，重新部署会清空数据——**仅用于验证，不存重要内容**。

## 2. 验证话术（3 个场景，请按原文发送）

每个场景**新建一个项目**（右上角 + 号），把 AI 的**完整回复原文**贴回聊天，我来判分。

**场景 A：没货源**（期望：梳理起步路径，不追问商品规格）

1. 建项目时需求描述填：`我想卖茶叶，但还没货源。`
2. 看 AI 第一问，正常回一句（比如有没有时间/资金情况），再看第二轮。

**场景 B：三个月没订单**（期望：先诊断，不默认重生成标题）

1. 建项目时需求描述填：`店开了三个月，没订单。`
2. 按 AI 的问题如实回一句（访客/来源可现编，如"每天几十个访客，都是自然搜索"），再看第二轮。

**场景 C：不知道预算**（期望：条件化建议+选项，不强迫填数字）

1. 建项目时需求描述填：`我想开个网店，但我不知道预算应该多少。`
2. 选 AI 给的一个选项或追问一句，看第三轮是否还咬住不放。

## 3. 判分标准（先给你，我收到记录后逐条打分）

- A：不问商品名/价格/规格等上架细节；给出"找产品 vs 先准备"类路径；每轮最多一两个问题。
- B：先问经营时长/访客/来源/变化点；不提"重新生成标题"；没数据时只给诊断思路。
- C：给出不同档位+取舍；不强迫填数字；"也许/大概"不被当成已定预算。
- 通用：已知信息不重复问；简洁中文；不出现"我已发布/已调价"类冒充。

## 4. 常见问题

- **一直转圈/报错 MODEL_API_KEY**：Render 面板 Environment 里检查 Key 是否填对，改完点 **Save → 服务会自动重部署**。
- **429 超预算**：累计 token 用完 100 万（正常验证只需几万），面板里调大 `MODEL_BUDGET_TOKENS` 即可。
- **504 超时**：点输入框上的**重试**按钮；DeepSeek 偶发排队，多试一次。
- **页面空白**：等 1 分钟（免费版唤醒中），刷新再试。

## 5. 记忆 v2 验证(静默记忆,无确认流程)

Render 会自动同步最新代码并重部署。打开网址验证(前两条建议新开项目 shared memory 互相干扰小，可同一项目连续发以观察冲突过期)：

1. 新建项目，需求描述填：`也许预算两万，先做代发试试。`
2. 点开对话上方的**项目档案**：应直接看到 1 条决定(预算两万)，**没有任何"待确认"和确认按钮**。
3. 聊一句：`预算改成五千。`再多聊一轮让 AI 回复带上记忆(看 AI 是否自然记得预算)。
4. 再看档案：决定应变成五千(旧的两万自动进历史，不展示)。
5. 说一句：`你记错了，我预算是一万不是五千。`看 AI 后续是否按一万回应，档案是否更新。
6. **刷新页面**：档案内容不变即通过。

把档案区截图或文字贴回，即可验收。
